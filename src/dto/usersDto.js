class usersDto {
    constructor(email, password, name, role, token) {
        this.email = email;
        this.password = password;
        this.name = name;
        this.role = role;
        this.token = token;
    }

};

export default usersDto;